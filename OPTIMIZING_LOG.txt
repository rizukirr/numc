================================================================================
                          numc Optimization Log
================================================================================
Records every performance optimization applied, what changed, and the results.
Newest entries at the bottom.

================================================================================
[2026-03-12] OMP Threshold + f64 MC Tuning + gemmsup K-Unroll
================================================================================
Commit: fa56f43
Files:  src/matmul/matmul.c, src/intrinsics/gemm_avx2.h

Changes:
  1. Lowered OMP parallelization threshold for matmul
  2. Tuned f64 MC blocking parameter (MC=72)
  3. Unrolled gemmsup (small-matrix) K-loop

Before (vs NumPy/OpenBLAS):
  f32 64x64:    ~0.80x
  f64 64x64:    ~0.70x
  f32 128x128:  ~0.85x
  f64 128x128:  ~0.80x

After:
  f32 64x64:    1.04x  (+30%)
  f64 64x64:    1.02x  (+46%)
  f32 128x128:  0.97x  (+14%)
  f64 128x128:  1.04x  (+30%)
  f32 256x256:  0.77x
  f64 256x256:  0.91x
  f32 512x512:  0.89x
  f64 512x512:  0.83x
  f32 1024x1024: 0.73-0.80x
  f64 1024x1024: 0.76-0.88x

Result: Small matrices (<=128) now match or beat NumPy. Large matrices still
        have a gap due to micro-kernel compute throughput.

Tests: 45/45 passed (native, NEON, RVV)

================================================================================
[2026-03-13] F32 ASM 8x K-Unroll + Gemmsup OMP Parallelism
================================================================================
Commit: (pending)
Files:  src/intrinsics/gemm_avx2.h, src/intrinsics/gemmsup_avx2.h,
        src/matmul/matmul.c

Problem:
  1. The f32 ASM kernel had only 4x K-unroll with 1 prefetch (A only), while
     the f64 kernel had 8x K-unroll with 3 prefetches. This caused more loop
     overhead and L1 cache misses on the B panel for f32 at 256x256+.
  2. 128x128 sat exactly at the gemmsup boundary (m*k*n < 128^3), falling to
     packed GEMM where packing overhead + OMP scheduling variance caused
     unstable results (28us to 136us across runs).
  3. Gemmsup kernels had no OMP parallelism, making them slower than numpy's
     multi-threaded BLAS at 128x128.

Changes:
  1. [gemm_avx2.h] Added GEMM_F32_ASM_K_ITER macro — extracts the 20-instr
     single K-iteration (matching f64's GEMM_F64_ASM_K_ITER pattern)
  2. [gemm_avx2.h] Rewrote GCC/Clang ASM K-loop: shr $2 -> shr $3 (8x unroll)
     - 3 prefetches per 8 iterations:
       * prefetcht0 384(%[ap])  — A panel ahead
       * prefetcht0 512(%[bp])  — B panel ahead (was missing entirely)
       * prefetcht0 768(%[ap])  — A panel further ahead
     - k_left mask: and $3 -> and $7
  3. [gemm_avx2.h] Updated MSVC intrinsics fallback: kc/4 -> kc/8, added
     B prefetch and second A prefetch to match ASM path
  4. [matmul.c] Changed gemmsup threshold from < to <= so 128x128 (exactly
     128^3 FLOPs) routes to the unpacked gemmsup path instead of packed GEMM
  5. [gemmsup_avx2.h] Added OMP parallel for to f32 and f64 gemmsup dispatch
     loops (parallelizes over MR-row tiles, threshold: M*K*N > 1<<20)

Before (vs NumPy/OpenBLAS, multi-threaded):
  f32 64x64:     1.04x
  f32 128x128:   0.97x (but unstable: 0.14x-0.97x due to OMP variance)
  f32 256x256:   0.77x
  f32 512x512:   0.89x
  f32 1024x1024: 0.73-0.80x

After (representative results, OMP_PROC_BIND=close):
  f32 64x64:     1.00x  (stable)
  f32 128x128:   0.80x  (single bench run; best observed 1.62x)
  f32 256x256:   1.03x  (+34% improvement)
  f32 512x512:   0.69-1.23x  (high variance from system load)
  f32 1024x1024: 0.87-1.10x  (high variance from system load)

  f64 64x64:     0.99x
  f64 128x128:   1.54x
  f64 256x256:   1.03-2.60x  (depends on system load)
  f64 512x512:   0.70-1.51x
  f64 1024x1024: 0.68-0.87x

  Single-threaded verification (eliminates OMP variance):
  f32 128x128:   numc 44us vs numpy 43us → 1.02x (parity)

Note: Benchmark results have high run-to-run variance due to system load
  during testing. The 8x K-unroll change primarily benefits 256x256+ sizes
  by reducing loop overhead and improving L1 prefetching. The gemmsup OMP
  change eliminates the packed GEMM scheduling variance for 128x128.

Tests: 45/45 passed (native x86, NEON cross-compile, RVV cross-compile)

================================================================================
[2026-03-13] AVX2 Emulated 64-bit Integer Min/Max for Reductions
================================================================================
Commit: (pending)
Files:  src/intrinsics/reduce_avx2.h, src/reduction/helpers.h,
        src/reduction/max.c, src/reduction/min.c

Problem:
  AVX2 lacks native 64-bit integer min/max instructions (vpminsq/vpmaxsq are
  AVX-512 only). The 64-bit int/uint min/max reductions (full, axis=0, axis=1)
  were using scalar fallbacks while 8/16/32-bit types had AVX2 SIMD kernels.
  This caused i64/u64 axis-1 reductions to run at 0.46-0.55x vs NumPy.

Changes:
  1. [reduce_avx2.h] Added emulated 64-bit min/max using vpcmpgtq + vpblendvb:
     - _mm256_max/min_epi64_emu (signed) — compare-and-blend
     - _mm256_max/min_epu64_emu (unsigned) — XOR sign-bit trick + compare
  2. [reduce_avx2.h] Added horizontal reductions (_hmax/hmin_epi64/epu64_avx2)
  3. [reduce_avx2.h] Added full array reductions via DEFINE_REDUCE_FULL_64_AVX2
  4. [reduce_avx2.h] Added fused row-reduce via DEFINE_FUSED_64_AVX2 macro
  5. [helpers.h] Added AVX2 wrappers for _vec_max/min_i64/u64
  6. [max.c, min.c] Wired _max/min_fused_i64/u64_avx2 into dispatch tables

Before (vs NumPy, 1000x1000 axis=1):
  MAX i64:  ~0.55x    MIN i64:  ~0.55x
  MAX u64:  ~0.46x    MIN u64:  ~0.46x

After (vs NumPy, 1000x1000):
  Full reduction:
    MAX i64: 1.90x    MIN i64: 3.26x
    MAX u64: 2.51x    MIN u64: 2.71x
  Axis=0:
    MAX i64: 1.82x    MIN i64: 1.85x
    MAX u64: 1.37x    MIN u64: 1.36x
  Axis=1:
    MAX i64: 1.24x    MIN i64: 1.26x
    MAX u64: 1.25x    MIN u64: 1.26x

Result: All 64-bit integer min/max reductions now beat NumPy across all
        reduction modes. The emulated vpcmpgtq+vpblendvb approach processes
        4 int64s per cycle vs 1 for scalar.

Tests: 45/45 passed (native x86, NEON cross-compile, RVV cross-compile)

================================================================================
[2026-03-13] Multi-ISA SIMD Min/Max Reduction Kernels
================================================================================
Commit: (pending)
Files:  src/intrinsics/reduce_avx512.h (new), src/intrinsics/reduce_neon.h,
        src/intrinsics/reduce_sve.h, src/intrinsics/reduce_rvv.h,
        src/reduction/helpers.h, src/reduction/max.c, src/reduction/min.c,
        CLAUDE.md

Problem:
  The SIMD min/max reduction kernels only existed for AVX2. The NEON, SVE,
  and RVV headers were empty TODO stubs, and there was no AVX-512 header.
  On non-x86 platforms, all integer min/max reductions fell back to scalar.

Changes:
  1. [reduce_avx512.h] New file — full AVX-512 implementation for all 8
     integer types (i8-u64). Native 512-bit vectors, native 64-bit min/max
     (vpminsq/vpmaxsq), native horizontal reduce (_mm512_reduce_*).
     64 elements/vector for i8, 8 for i64.
  2. [reduce_neon.h] Full NEON implementation for all 8 integer types.
     128-bit vectors, native 8/16/32-bit min/max (vmaxq_s8 etc), emulated
     64-bit via vcgtq_s64+vbslq_s64. Native horizontal reduce (vmaxvq_s8).
  3. [reduce_sve.h] Full SVE implementation for all 8 integer types.
     Scalable vectors with predication, native min/max for ALL widths
     including 64-bit (svmax_s64_x), native horizontal reduce (svmaxv_s64).
     SVE predication handles tails — no scalar cleanup needed.
  4. [reduce_rvv.h] Full RVV implementation for all 8 integer types.
     Scalable vectors with vsetvl, native min/max for ALL widths including
     64-bit (__riscv_vmax_vv_i64m1), native horizontal reduce
     (__riscv_vredmax_vs_i64m1_i64m1). Fixed tail bug: use vl not vlmax
     for element-wise comparison to avoid reading undefined tail elements.
  5. [helpers.h] Restructured dispatch chain: AVX-512 > AVX2 > SVE > NEON
     > RVV > scalar. Unified _VEC_WRAP macro for all ISA wrappers.
  6. [max.c, min.c] Expanded fused dispatch tables for all 6 ISAs.
  7. [CLAUDE.md] Added rule: every SIMD intrinsic must be implemented for
     ALL supported ISAs at the same time — no TODO stubs.

Each ISA header provides 32 functions: 8 types × {min,max} × {full,fused}.

Tests: 45/45 passed (native x86, NEON, SVE, RVV)

================================================================================
[2026-03-13] SIMD Scalar Comparison Kernels — All ISAs, All Types
================================================================================
Commit: (pending)
Files:  src/intrinsics/compare_scalar_avx2.h (new),
        src/intrinsics/compare_scalar_avx512.h (new),
        src/intrinsics/compare_scalar_neon.h (new),
        src/intrinsics/compare_scalar_sve.h (new),
        src/intrinsics/compare_scalar_rvv.h (new),
        src/elemwise/compare.c

Problem:
  Scalar comparisons (eq/gt/lt/ge/le_scalar) relied on auto-vectorization
  via `#pragma omp simd` in the generic DEFINE_BINARY_KERNEL macro's
  `sb == 0` path. No dedicated SIMD intrinsics existed for scalar compares.
  Integer types were especially slow because the compiler couldn't optimize
  the unsigned XOR-bias trick or certain comparison patterns.

  f32 scalar comparisons: ~0.60x vs NumPy
  f64 scalar comparisons: ~0.80x vs NumPy
  Integer types: varied, many below 1.0x

Changes:
  1. [compare_scalar_avx2.h] New — 50 functions (10 types × 5 ops).
     Float: _mm256_cmp_ps/pd with IMM predicates, AND with 1.0.
     Signed int: _mm256_cmpeq/cmpgt_epiW, AND with 1.
     Unsigned int: XOR sign-bit bias + signed compare (pre-computed
     biased scalar outside loop for efficiency).

  2. [compare_scalar_avx512.h] New — 50 functions.
     Float: _mm512_cmp_ps/pd_mask + _mm512_mask_blend.
     Signed int: _mm512_cmp_epiW_mask with _MM_CMPINT predicates +
     _mm512_maskz_set1. Native 64-bit compare (no emulation).
     Unsigned int: _mm512_cmp_epuW_mask — native unsigned compare.

  3. [compare_scalar_neon.h] New — 50 functions.
     All types have native NEON comparisons (vceqq, vcgtq, vcgeq,
     vcltq, vcleq for s/u/f types). Float uses vbslq for 0/1 select.

  4. [compare_scalar_sve.h] New — 50 functions.
     Predicated loops with svwhilelt. Scalar-immediate compare
     variants (svcmpeq_n_*) avoid explicit broadcast. svsel for 0/1.

  5. [compare_scalar_rvv.h] New — 50 functions.
     Vector-scalar compare intrinsics (_vx/_vf) for efficiency.
     LMUL=m4. vmerge/vfmerge for 0/1 conversion.

  6. [compare.c] Added CmpScKern function pointer tables per op,
     ISA-selected via CMPSC() macro. New DEFINE_CMP_SCALAR_WITH_SIMD
     macro intercepts contiguous scalar comparisons and dispatches to
     SIMD kernels. OMP parallelization for large arrays (>512KB per
     thread) splits work into chunks, each processed by SIMD kernel.
     Falls back to generic _scalar_op for non-contiguous arrays.

Before (vs NumPy, 1M elements):
  f32 scalar eq/gt/lt/ge/le:  ~0.60x
  f64 scalar eq/gt/lt/ge/le:  ~0.80x
  i8:   ~1.0x    u8:  ~1.0x
  i16:  ~1.0x    u16: ~1.0x
  i32:  ~1.0x    u32: ~1.0x
  i64:  ~0.90x   u64: ~0.90x

After (vs NumPy, 1M elements, representative):
  f32:  1.3–4.5x   (best: u32 eq 4.04x)
  f64:  0.62–0.89x (limited by 8B output vs NumPy's 1B bool)
  i8:   5.9–8.2x
  i16:  8.7–10.3x
  i32:  9.3–15.6x
  i64:  1.5–1.8x
  u8:   6.9–8.3x
  u16:  9.0–10.3x
  u32:  12.4–16.6x
  u64:  2.0–2.4x

Note: f64 remains below 1.0x because numc outputs float64 (8 bytes per
  element) for comparison results while NumPy outputs bool (1 byte).
  This is 8x more memory traffic — an architectural difference, not a
  kernel performance issue.

Each ISA header provides 50 functions: 10 types × 5 ops (eq/gt/lt/ge/le).

Tests: 45/45 passed (native x86, NEON, SVE, RVV)
